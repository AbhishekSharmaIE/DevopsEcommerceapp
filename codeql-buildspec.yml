version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - echo "Running dummy CodeQL check..."

  build:
    commands:
      - echo "CodeQL analysis skipped - dummy check passed"
      - echo "Proceeding with build and deploy..."

artifacts:
  files:
    - '**/*'
  name: CodeQLArtifact 